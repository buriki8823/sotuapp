<footer class="footer mt-auto d-flex align-items-center" style="background: linear-gradient(to right, #34C8E2); height: 50px;">
  <%= link_to '利用規約', terms_path, class: 'mb-0 text-white ms-4 text-decoration-none' %>
  <%= link_to 'プライバシーポリシー', privacy_policy_path, class: 'mb-0 text-white ms-4 text-decoration-none' %>
  <%= link_to '使い方ガイド', tukaikata_path, class: 'mb-0 text-white ms-4 text-decoration-none' %>
</footer>

<style>
/* デフォルト（スマホ想定: 768px以下） */
.footer {
  background: linear-gradient(to right, #34C8E2);
  height: 50px;
  display: flex;
  align-items: center;
  justify-content: center; /* 中央寄せ */
}

.footer a {
  font-size: 14px;
  margin-left: 8px;
}

/* タブレット以上（769px以上） */
@media (min-width: 769px) {
  .footer {
    justify-content: flex-start; /* 左寄せに変更 */
    padding-left: 20px;
  }

  .footer a {
    font-size: 16px;
    margin-left: 24px;
  }
}
</style>