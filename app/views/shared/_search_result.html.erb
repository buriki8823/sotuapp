<div class="search-result-card border rounded p-3 mb-4 d-flex flex-wrap" style="min-height: 200px;">
  <!-- 左側：アイコン＋アカウント名 -->
  <div class="user-info d-flex flex-column align-items-center justify-content-center" style="flex: 0 0 30%;">
    <%= image_tag(post.user.avatar.attached? ? url_for(post.user.avatar) : "default_avatar.png", class: "rounded-circle mb-2", size: "60x60") %>
    <span class="fw-bold text-center"><%= post.user.username %></span>
  </div>

  <!-- 右側：投稿画像 -->
  <div class="post-image" style="flex: 0 0 70%;">
    <%= image_tag(post.image.attached? ? url_for(post.image) : "no_image.png", class: "img-fluid h-100 w-100 object-fit-cover rounded") %>
  </div>
</div>