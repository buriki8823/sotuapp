const kind = "<%= params[:kind] %>";
const button = document.querySelector(`button[data-kind='${kind}']`);

if (button) {
  button.classList.toggle("btn-success");
  button.classList.toggle("btn-outline-secondary");
}